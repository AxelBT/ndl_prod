<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Profil — Résistance NIRD</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Tailwind CSS -->
  <link href="/css/output.css" rel="stylesheet">

  <style>
    /* Hover léger sur les étapes */
    .step:hover {
      background-color: #e6ffed;
      transform: translateY(-2px);
      transition: all 0.2s ease-in-out;
    }
  </style>
</head>

<body class="bg-gray-50 text-gray-800 min-h-screen font-sans">

  <!-- Conteneur principal -->
  <div class="max-w-5xl mx-auto py-12 px-6">

    <!-- Titre -->
    <h1 class="text-4xl md:text-5xl font-bold text-center text-green-700 mb-12">
      Bienvenue Résistant · <%= roleData.label %>
    </h1>

    <!-- Introduction personnalisée -->
    <div class="bg-white p-6 rounded-lg shadow-md border border-gray-200 mb-12">
      <% if (role === "etablissement") { %>
        <p class="text-lg">
          🎓 En tant qu'Établissement Résistant, vous guidez élèves et équipes vers un numérique inclusif, souverain et durable.
        </p>
      <% } else if (role === "collectivite") { %>
        <p class="text-lg">
          🏛️ Collectivité Engagée : vous contribuez à un numérique public responsable, aidant de nombreux établissements à se libérer de l'obsolescence programmée.
        </p>
      <% } else { %>
        <p class="text-lg">
          👤 Résistant Libre : chaque geste compte. Vous êtes l’acteur direct du libre et de la sobriété numérique au quotidien.
        </p>
      <% } %>
    </div>

    <!-- SECTION: Missions -->
    <h2 class="text-2xl font-bold text-green-700 mb-6">🎯 Missions clés</h2>

    <div class="space-y-6">
      <% missions.forEach((mission, index) => { %>
        <div class="step flex items-start bg-white p-5 rounded-lg shadow-sm border border-gray-200">
          <div class="flex-shrink-0 w-8 h-8 flex items-center justify-center rounded-full bg-green-700 text-white font-bold mr-4">
            <%= index + 1 %>
          </div>
          <p class="text-gray-800 text-lg"><%= mission %></p>
        </div>
      <% }); %>
    </div>

    <!-- SECTION: Badges -->
    <h2 class="text-2xl font-bold text-green-700 mt-12 mb-6">🏅 Badges NIRD</h2>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <div class="bg-white p-4 text-center border border-gray-200 rounded-lg shadow-sm">
        🐧<br>Pingouin Initié
      </div>
      <div class="bg-white p-4 text-center border border-gray-200 rounded-lg shadow-sm">
        ♻️<br>Reconditionneur
      </div>
      <div class="bg-white p-4 text-center border border-gray-200 rounded-lg shadow-sm">
        🔐<br>Souveraineté Numérique
      </div>
      <div class="bg-white p-4 text-center border border-gray-200 rounded-lg shadow-sm">
        🌱<br>Acteur de la Sobriété
      </div>
    </div>

    <!-- SECTION: Liens écosystème NIRD -->
    <h2 class="text-2xl font-bold text-green-700 mt-12 mb-6">🔗 Rejoindre l’écosystème NIRD</h2>

    <div class="grid md:grid-cols-2 gap-4">
      <% if (roleData.links) { %>
        <% for (const [key, link] of Object.entries(roleData.links)) { %>
          <a href="<%= link %>" target="_blank" class="block bg-white p-5 border border-gray-200 rounded-lg shadow-sm hover:bg-green-50 transition">
            <%= key.charAt(0).toUpperCase() + key.slice(1) %>
          </a>
        <% } %>
      <% } %>
    </div>

  </div>

</body>
</html>
